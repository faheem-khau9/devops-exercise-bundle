# Local-stage environment overlay.
# Do not remove resources.limits, securityContext.runAsNonRoot, or the image tag —
# the shipped Conftest helm policy will reject the rendered output if they are missing.

replicaCount: 1

image:
  repository: traefik/whoami
  tag: v1.10.4
  pullPolicy: IfNotPresent

# Container listens on 8080; Service maps 80→8080 via the named "http" port.
containerPort: 8080

args:
  - --port
  - "8080"

resources:
  limits:
    cpu: 200m
    memory: 64Mi
  requests:
    cpu: 50m
    memory: 32Mi

securityContext:
  runAsNonRoot: true
  runAsUser: 65532
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

envFromSecret: "sample-app-secret"

labels:
  environment: local-stage
